name: Render CV and Deploy to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - 'CV/**'
      - 'portfolio/**'
      - 'index.html'
      - 'sidebar.html'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  render-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare output directory
        run: |
          mkdir -p _site/CV _site/portfolio
          cp index.html _site/index.html
          cp sidebar.html _site/sidebar.html
          # Copy all portfolio files
          cp -r portfolio/portfolio.html _site/portfolio/
          cp -r portfolio/projects.json _site/portfolio/
          # Copy portfolio stylesheet so project pages resolve ../../styles.css correctly
          cp -r portfolio/styles.css _site/portfolio/styles.css
          mkdir -p _site/portfolio/projects
          cp -r portfolio/projects/* _site/portfolio/projects/
          find CV -name "*.html" -exec cp {} _site/CV/Boonyakorn_Tanrattanakorn_CV.html \;
          find CV -name "*.pdf" -exec cp {} _site/CV/Boonyakorn_Tanrattanakorn_CV.pdf \; || true

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
